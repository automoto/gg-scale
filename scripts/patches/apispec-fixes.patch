diff --git a/internal/spec/extractor.go b/internal/spec/extractor.go
index 06de852..d48e1bc 100644
--- a/internal/spec/extractor.go
+++ b/internal/spec/extractor.go
@@ -834,6 +834,17 @@ func (e *Extractor) findTargetNode(assignment *metadata.CallArgument) TrackerNod
 
 // extractRouteChildren extracts request, response, and params from children nodes
 func (e *Extractor) extractRouteChildren(routeNode TrackerNodeInterface, route *RouteInfo, mountTags []string, routes *[]*RouteInfo, visitedEdges map[string]bool) {
+	e.extractRouteChildrenGuarded(routeNode, route, mountTags, routes, visitedEdges, map[TrackerNodeInterface]bool{})
+}
+
+// extractRouteChildrenGuarded is extractRouteChildren with a visited-node set:
+// tracker trees can contain cycles (recursive functions in the analyzed code),
+// which previously recursed without bound and crashed with a stack overflow.
+func (e *Extractor) extractRouteChildrenGuarded(routeNode TrackerNodeInterface, route *RouteInfo, mountTags []string, routes *[]*RouteInfo, visitedEdges map[string]bool, visitedNodes map[TrackerNodeInterface]bool) {
+	if visitedNodes[routeNode] {
+		return
+	}
+	visitedNodes[routeNode] = true
 	for _, child := range routeNode.GetChildren() {
 		// Check for route patterns in children nodes
 		if isRoute := e.executeRoutePattern(child, route); isRoute {
@@ -864,7 +875,7 @@ func (e *Extractor) extractRouteChildren(routeNode TrackerNodeInterface, route *
 		}
 
 		// Recursive extraction
-		e.extractRouteChildren(child, route, mountTags, routes, visitedEdges)
+		e.extractRouteChildrenGuarded(child, route, mountTags, routes, visitedEdges, visitedNodes)
 	}
 
 	// Extract parameters from the route node itself
diff --git a/internal/spec/mapper.go b/internal/spec/mapper.go
index ffcda3b..32a468b 100644
--- a/internal/spec/mapper.go
+++ b/internal/spec/mapper.go
@@ -540,8 +540,27 @@ func buildResponses(respInfo map[string]*ResponseInfo) map[string]Response {
 	// traced through an indirection helper). Drop it in those cases; keep a
 	// default whose body is concrete and distinct — that's a real response
 	// whose status genuinely couldn't be resolved.
-	if def := chosen["default"]; def != nil && len(chosen) > 1 && uninformativeDefault(def, chosen) {
+	// Normalize the "status could not be determined" default against net/http
+	// semantics: writing a body without WriteHeader IS a 200, so when no 2xx
+	// was resolved the default is the operation's success response — even a
+	// generic `object` body marks the success path. When a 2xx WAS resolved,
+	// the same handler cannot also implicitly 200: a leftover default is
+	// either a duplicate of the resolved body or a body reached through an
+	// unrelated tracker path (shared helper visited from another route); drop
+	// it either way.
+	if def := chosen["default"]; def != nil {
+		has2xx := false
+		for status := range chosen {
+			if len(status) == 3 && status[0] == '2' {
+				has2xx = true
+				break
+			}
+		}
 		delete(chosen, "default")
+		if !has2xx {
+			def.StatusCode = http.StatusOK
+			chosen["200"] = preferResponseInfo(chosen["200"], def)
+		}
 	}
 
 	for statusCode, resp := range chosen {

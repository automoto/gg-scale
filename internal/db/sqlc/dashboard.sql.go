// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: dashboard.sql

package sqlcgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createProjectForTenant = `-- name: CreateProjectForTenant :one
INSERT INTO projects (tenant_id, name)
VALUES (
    current_setting('app.tenant_id', true)::bigint,
    trim($1::text)
)
RETURNING id, name, created_at
`

type CreateProjectForTenantRow struct {
	ID        int64
	Name      string
	CreatedAt pgtype.Timestamptz
}

func (q *Queries) CreateProjectForTenant(ctx context.Context, name string) (CreateProjectForTenantRow, error) {
	row := q.db.QueryRow(ctx, createProjectForTenant, name)
	var i CreateProjectForTenantRow
	err := row.Scan(&i.ID, &i.Name, &i.CreatedAt)
	return i, err
}

const dashboardCreateTenant = `-- name: DashboardCreateTenant :one
SELECT
    r.tenant_id::bigint AS tenant_id,
    r.project_id::bigint AS project_id,
    r.api_key_id::bigint AS api_key_id,
    r.membership_id::bigint AS membership_id
FROM dashboard_create_tenant(
    $1,
    $2,
    $3,
    $4,
    $5
) AS r(tenant_id, project_id, api_key_id, membership_id)
`

type DashboardCreateTenantParams struct {
	ActorUserID int64
	TenantName  string
	ProjectName string
	KeyHash     []byte
	KeyLabel    string
}

type DashboardCreateTenantRow struct {
	TenantID     int64
	ProjectID    int64
	ApiKeyID     int64
	MembershipID int64
}

func (q *Queries) DashboardCreateTenant(ctx context.Context, arg DashboardCreateTenantParams) (DashboardCreateTenantRow, error) {
	row := q.db.QueryRow(ctx, dashboardCreateTenant,
		arg.ActorUserID,
		arg.TenantName,
		arg.ProjectName,
		arg.KeyHash,
		arg.KeyLabel,
	)
	var i DashboardCreateTenantRow
	err := row.Scan(
		&i.TenantID,
		&i.ProjectID,
		&i.ApiKeyID,
		&i.MembershipID,
	)
	return i, err
}

const listProjectsForTenant = `-- name: ListProjectsForTenant :many
SELECT id, name, created_at
FROM projects
WHERE tenant_id = current_setting('app.tenant_id', true)::bigint
  AND deleted_at IS NULL
ORDER BY name
`

type ListProjectsForTenantRow struct {
	ID        int64
	Name      string
	CreatedAt pgtype.Timestamptz
}

func (q *Queries) ListProjectsForTenant(ctx context.Context) ([]ListProjectsForTenantRow, error) {
	rows, err := q.db.Query(ctx, listProjectsForTenant)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListProjectsForTenantRow
	for rows.Next() {
		var i ListProjectsForTenantRow
		if err := rows.Scan(&i.ID, &i.Name, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
